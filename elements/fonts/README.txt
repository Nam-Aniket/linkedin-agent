Drop your display-font TTF here (see ONBOARDING.md step 4) and point
profile.json design.fonts.display_file at it. Use a font whose license
allows embedding/redistribution (OFL fonts from Google Fonts are safe).
