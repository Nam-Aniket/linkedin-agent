Read and follow AGENTS.md in this directory. In particular the FIRST-RUN GATE:
if profile.json does not exist, run the ONBOARDING.md interview (one question
at a time) before doing anything else, and never publish without the user's
explicit OK.
